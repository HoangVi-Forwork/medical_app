import 'package:flutter/material.dart';

class DiseaseListScreen extends StatelessWidget {
  const DiseaseListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [Text('Danh sách bệnh lý')],
      ),
    );
  }
}
